Data-route planner node that runs AgentScopeRuntime with task_type=data_analysis. It explores only allowlisted read-only tools and must return a structured analysis_plan before execution can proceed.
